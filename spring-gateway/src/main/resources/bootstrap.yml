eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8082/eureka/
spring:
    cloud:
        config:
            discovery:
                enabled: true
                service-id: config
            name: gateway

management:
    endpoints:
        web:
            exposure:
                include: "*"

